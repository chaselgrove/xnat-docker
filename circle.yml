machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.3
  services:
    - docker
dependencies:
  override:
    - docker build -t xnat builder/
